#!/usr/bin/env python3
"""Entry point: run from a checkout without installing anything."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))

from cost_meter.cli import main  # noqa: E402

sys.exit(main())
